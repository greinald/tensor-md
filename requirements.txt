# Patch Learning for Outlier Detection of Tensor-Valued Data
# Python 3.11 or newer is recommended.  The version ranges below keep the
# numerical and CNN APIs stable while allowing platform-specific wheels.

# Numerical modelling and image handling
numpy>=1.26,<3
scipy>=1.11,<2
scikit-learn>=1.4,<1.8
Pillow>=10,<12
tifffile>=2023.7

# Evaluation and experiment utilities
tqdm>=4.66,<5
tabulate>=0.9,<1
matplotlib>=3.8,<4
plotly>=5.18,<7
pandas>=2.1,<3
seaborn>=0.13,<1

# Notebook execution and reproducible kernels
jupyterlab>=4,<5
ipykernel>=6.25,<7

# CNN feature extraction.  On Apple Silicon, see the platform note in
# REPRODUCIBILITY.md before installing TensorFlow.
torch>=2.3,<2.9
torchvision>=0.18,<0.24
tensorflow>=2.16,<2.19

# Optional detector/preprocessing used only by YOLO experiments.
ultralytics>=8.3,<9
